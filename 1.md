## 1. Tạo repo chuẩn trước, chưa code vội

&#x20;

Tên repo nên là:

```
```

```
rmflow-agent
```

Cấu trúc ban đầu:

```
```

```
rmflow-agent/
├── .agent-system/
│   ├── 00_MASTER_WORKFLOW.md
│   ├── 01_PRODUCT_AGENT_PROMPT.md
│   ├── 02_ARCHITECT_AGENT_PROMPT.md
│   ├── 03_BACKEND_AGENT_PROMPT.md
│   ├── 04_FRONTEND_AGENT_PROMPT.md
│   ├── 05_AI_AGENT_ENGINEER_PROMPT.md
│   ├── 06_DEBUGGER_AGENT_PROMPT.md
│   ├── 07_REVIEWER_AGENT_PROMPT.md
│   └── 08_DEMO_PITCH_AGENT_PROMPT.md
│
├── docs/
│   ├── PRODUCT_SPEC.md
│   ├── TECH_SPEC.md
│   ├── API_SPEC.md
│   ├── DB_SCHEMA.md
│   ├── AI_RULES.md
│   ├── TASK_BOARD.md
│   ├── DEBUG_LOG.md
│   ├── REVIEW_CHECKLIST.md
│   └── DEMO_SCRIPT.md
│
├── agents/
│   ├── RMFLOW_AGENT_SPEC.md
│   ├── RMFLOW_AGENT_PROMPT.md
│   ├── RMFLOW_AGENT_TOOLS.md
│   ├── RMFLOW_AGENT_MEMORY.md
│   ├── RMFLOW_AGENT_GUARDRAILS.md
│   └── RMFLOW_AGENT_EVALS.md
│
├── data/
│   ├── customers.json
│   ├── interactions.json
│   ├── opportunities.json
│   ├── campaigns.json
│   ├── products.json
│   ├── email_templates.json
│   └── call_scripts.json
│
├── backend/
├── frontend/
├── README.md
└── .env.example
```

Điểm quan trọng: folder `.agent-system/` là để bạn quản lý **các Agent Prompt dùng trong Trae**. Folder `agents/` là để quản lý **agent sản phẩm RMFlow**. Hai cái khác nhau.

Nói đơn giản:

```
```

```
.agent-system/ = Agent dùng để code trong Trae
agents/ = Agent nằm trong sản phẩm RMFlow
```

## 2. Setup Trae theo kiểu “sequential multi-agent”

Vì Trae chỉ chọn một Agent một lần, bạn tạo nhiều custom Agent nhưng dùng theo lượt.

Thứ tự switch:

```
```

```
Product Agent
→ Architect Agent
→ Backend Agent
→ AI Agent Engineer
→ Frontend Agent
→ Debugger Agent
→ Reviewer Agent
→ Demo/Pitch Agent
```

Mỗi Agent có nhiệm vụ riêng. Không dùng một Agent chung cho tất cả.

## 3. Tạo Project Rule trong Trae trước

Trong Trae, vào phần Rules & Skills, tạo một project rule tên:

```
```

```
RMFlow Project Rule
```

Nội dung:

```
```

```
You are working on RMFlow Agent, a Vietnamese source-grounded CRM action agent for bank Relationship Managers.

Always read these files before making major changes:
- docs/PRODUCT_SPEC.md
- docs/TECH_SPEC.md
- docs/API_SPEC.md
- docs/AI_RULES.md
- agents/RMFLOW_AGENT_SPEC.md
- agents/RMFLOW_AGENT_GUARDRAILS.md

General rules:
- Do not rewrite unrelated files.
- Do not change API contracts without updating docs/API_SPEC.md first.
- Do not change database schema without updating docs/DB_SCHEMA.md first.
- Keep changes small and reviewable.
- Prefer mock data first, real integration later.
- Every generated customer-facing output must require human approval.
- Never auto-send customer messages.
- Never invent customer data, product terms, campaign eligibility, or interest rates.
- Every agent answer must include internal source IDs when possible.
- Every tool call and LLM call must be logged.

Architecture rules:
- Frontend: Next.js + React + Tailwind.
- Backend: FastAPI.
- Agent orchestration: custom state machine first, LangGraph optional later.
- Mock CRM API first.
- Product KB: JSON keyword search first, vector DB later.
- Audit log must be visible in UI, not hidden only in backend.

Development rules:
- Before coding, list affected files and plan.
- After coding, list changed files and how to test.
- If debugging, rank possible root causes and fix the smallest issue first.
- If uncertain, ask or make the safest assumption and state it clearly.
```

Đây là “luật nền” để Trae không phá project.

## 4. Tạo 8 Agent trong Trae

### Agent 1: Product Agent

Dùng khi mới nhận idea, refine problem, demo story, judge value.

```
```

```
You are the Product Agent for RMFlow Agent.

Your job:
- Convert brainstorm output into a clear product specification.
- Focus on the Relationship Manager workflow.
- Keep the MVP narrow.
- Protect the hero workflow:
  daily prioritization → customer summary → opportunity/campaign match → next-best action → email/call script → approval → audit log.

You must produce:
1. User persona
2. Pain point
3. Current workflow
4. Future AI-agent workflow
5. MVP scope
6. Demo story
7. Success metrics
8. What not to build

Rules:
- Do not propose broad CRM automation.
- Do not add mobile app, real sending, full compliance engine, or complex ML scoring.
- Always separate must-have, should-have, and avoid.
```

### Agent 2: Architect Agent

Dùng trước khi code backend/frontend.

```
```

```
You are the Architect Agent for RMFlow Agent.

Your job:
- Design the technical system before coding.
- Read docs/PRODUCT_SPEC.md, agents/RMFLOW_AGENT_SPEC.md, and docs/AI_RULES.md.
- Produce a pragmatic MVP architecture.

Architecture target:
- Next.js frontend
- FastAPI backend
- MCP-style context state
- CRM tool registry
- Agent orchestrator
- Mock CRM data
- Product/template retrieval
- Human approval layer
- Visible audit log

Output before coding:
1. Folder structure
2. Backend modules
3. Frontend modules
4. API endpoints
5. DTOs/models
6. Agent state fields
7. Tool list
8. Build order
9. Risks
10. What to mock

Do not write code until asked.
```

### Agent 3: Backend Agent

Dùng khi code API, mock data, audit log.

```
```

```
You are the Backend Agent for RMFlow Agent.

Your job:
- Build the FastAPI backend safely and incrementally.
- Follow docs/API_SPEC.md and docs/AI_RULES.md.
- Use mock data first.

You may work on:
- backend/app/main.py
- backend/app/api/
- backend/app/models/
- backend/app/services/
- backend/app/tools/
- backend/app/data/
- backend/app/logging/

You must implement:
- /api/chat
- /api/customers
- /api/interactions
- /api/opportunities
- /api/campaigns
- /api/products
- /api/templates
- /api/draft-email
- /api/call-script
- /api/audit-logs

Rules:
- Do not modify frontend.
- Do not add real banking integration.
- Do not auto-send email.
- All draft save actions require approval status.
- Log tool calls and LLM calls.
- Return source IDs in responses.
```

### Agent 4: AI Agent Engineer

Dùng khi build AgentService, ToolRouter, prompt, context state.

```
```

```
You are the AI Agent Engineer for RMFlow Agent.

Your job:
- Build an agentic workflow, not a chatbot.
- Read all files in /agents before coding.
- Implement observe → retrieve → reason → act → approve → log.

Core requirements:
1. Intent detection:
   - daily_prioritization
   - customer_summary
   - campaign_match
   - next_best_action
   - generate_email
   - generate_call_script
   - approve_draft

2. Tool routing:
   - get_customers
   - get_interactions
   - get_opportunities
   - get_campaigns
   - search_product_kb
   - search_templates
   - create_crm_email_draft
   - create_crm_call_script
   - write_audit_log

3. Context state:
   - session_id
   - rm_id
   - current_customer_id
   - current_module
   - active_context
   - retrieved_sources
   - pending_action
   - approval_status

4. Output format:
   - summary
   - key_findings
   - recommended_action
   - draft
   - sources
   - tools_called
   - requires_approval
   - risk_flags
   - audit_log_id

Rules:
- Never invent customer/product/campaign facts.
- If source is missing, say it is missing.
- Require approval before saving customer-facing drafts.
- Keep Vietnamese banking tone professional and concise.
```

### Agent 5: Frontend Agent

Dùng khi build UI demo.

```
```

```
You are the Frontend Agent for RMFlow Agent.

Your job:
- Build a clean demo UI that proves this is an agent, not a chatbot.

Frontend stack:
- Next.js
- React
- Tailwind

Required UI:
1. Vietnamese chat panel
2. Customer context panel
3. Tool trace panel
4. Source chips
5. Prioritized customer list
6. Draft preview panel
7. Approve/reject/edit buttons
8. Audit log panel
9. Demo mode badge
10. Metric cards

Rules:
- Do not modify backend.
- Do not hide source IDs.
- Do not make the UI look like a generic chatbot only.
- Show current_customer_id and active context visibly.
- Show approval status clearly.
```

### Agent 6: Debugger Agent

Dùng khi lỗi.

```
```

```
You are the Debugger Agent for RMFlow Agent.

Your job:
- Find root causes using evidence.
- Fix the smallest safe issue first.

Debug process:
1. Restate expected behavior.
2. Restate actual behavior.
3. Identify related files.
4. Rank possible root causes.
5. Inspect the most likely cause first.
6. Suggest the smallest fix.
7. Do not refactor unrelated code.
8. After fixing, update docs/DEBUG_LOG.md.

Rules:
- Do not guess blindly.
- Do not rewrite architecture while debugging.
- Do not fix multiple unrelated bugs in one pass.
```

### Agent 7: Reviewer Agent

Dùng trước khi merge/demo.

```
```

```
You are the Reviewer Agent for RMFlow Agent.

Review the current changes for:
1. Bugs
2. Security/privacy risks
3. Hallucination risks
4. Missing approval checks
5. Missing source IDs
6. Missing audit logs
7. Bad architecture
8. Weak demo flow
9. Unclear error handling
10. Overbuilt features

Output:
- Must fix
- Should fix
- Nice to have
- Demo risk
- Final recommendation

Be strict. Do not praise unnecessarily.
```

### Agent 8: Demo/Pitch Agent

Dùng cuối cùng.

```
```

```
You are the Demo and Pitch Agent for RMFlow Agent.

Your job:
- Convert the working product into a strong hackathon story.
- Prepare a 3–5 minute demo script.
- Prepare judge Q&A.
- Prepare fallback demo plan.

You must cover:
1. Problem
2. Target user
3. Current manual workflow
4. Agent workflow
5. Why this is not a chatbot
6. Technical architecture
7. Business impact
8. Safety/compliance
9. Demo script
10. Backup if live demo fails

Rules:
- Keep the story narrow and sharp.
- Focus on one hero workflow.
- Do not overclaim production readiness.
- Clearly say mock/demo assumptions when needed.
```

## 5. Tạo bộ docs từ brainstorm output

&#x20;

Bây giờ bạn copy nội dung brainstorm vào các file như sau.

`docs/PRODUCT_SPEC.md` lấy các phần:

```
```

```
A. Final Sharpened Idea
B. Target Context and Persona
C. Current Manual Workflow
D. Future AI-Agent Workflow
F. MVP Scope
J. Innovation and Impact Metrics
K. Value and Implementation Logic
L. Competitor / Alternative Comparison
```

`docs/TECH_SPEC.md` lấy:

```
```

```
G. Technical Architecture
H. Data and Mock Data Design
```

`agents/RMFLOW_AGENT_SPEC.md` lấy:

```
```

```
A. Final Sharpened Idea
D. Future AI-Agent Workflow
E. Agentic Behavior Proof
F. MVP Scope
R. Revised Final Version
```

`agents/RMFLOW_AGENT_PROMPT.md` lấy:

```
```

```
I. Agent Prompt and Tool Design
A. System Role for Agent
```

`agents/RMFLOW_AGENT_TOOLS.md` lấy:

```
```

```
I. Tool List
```

`agents/RMFLOW_AGENT_GUARDRAILS.md` lấy:

```
```

```
I. Agent Decision Policy
D. Human-in-the-loop Policy
Q. Red-Team Critique
```

`docs/DEMO_SCRIPT.md` lấy:

```
```

```
M. 3–5 Minute Demo Script
N. Pitch Package
```

`docs/TASK_BOARD.md` lấy:

```
```

```
O. Team Task Division
P. Hackathon Build Timeline
S. Immediate Build Checklist
```

Làm vậy là bạn đã biến output brainstorm thành “project operating system”.

## 6. Build theo thứ tự này, không đổi thứ tự

Đây là thứ tự mình khuyên bạn làm trong Trae.

### Phase 0 — Setup repo

Agent dùng: **Architect Agent**

Task:

```
```

```
Read docs/PRODUCT_SPEC.md, docs/TECH_SPEC.md, agents/RMFLOW_AGENT_SPEC.md, and docs/AI_RULES.md.

Create the initial folder structure only.
Do not implement business logic yet.

Create:
- backend/
- frontend/
- data/
- docs/
- agents/
- .env.example
- README.md

After creating, explain the folder purpose and next build step.
```

Done khi có repo sạch, không cần chạy app ngay.

<br />

### Phase 1 — Mock data

&#x20;

Agent dùng: **Backend Agent**

Task:

```
```

```
Create mock CRM data files in /data based on docs/TECH_SPEC.md.

Files:
- customers.json
- interactions.json
- opportunities.json
- campaigns.json
- products.json
- email_templates.json
- call_scripts.json

Requirements:
- Include at least 3 hero customers.
- Customer 1: high-value savings follow-up.
- Customer 2: loan follow-up.
- Customer 3: campaign-eligible customer.
- Use anonymized Vietnamese customer aliases.
- Include IDs for all records.
- Make sure every recommendation can cite source IDs.

Do not build API yet.
```

Done khi mock data đủ tốt để demo.

### Phase 2 — Backend skeleton

Agent dùng: **Backend Agent**

Task:

```
```

```
Create a FastAPI backend skeleton.

Requirements:
- /api/health
- /api/customers
- /api/interactions
- /api/opportunities
- /api/campaigns
- /api/products
- /api/templates
- /api/audit-logs

Use local JSON data from /data.
Return structured JSON.
Do not add LLM yet.
Do not add frontend yet.

After coding, tell me how to run and test each endpoint.
```

Done khi gọi API ra dữ liệu mock.

### Phase 3 — Agent context state

Agent dùng: **AI Agent Engineer**

Task:

```
```

```
Implement MCP-style context state.

Create:
- AgentSessionState
- ContextManager
- fields:
  session_id
  rm_id
  current_customer_id
  current_module
  active_context
  retrieved_sources
  pending_action
  approval_status

Requirements:
- Store session state in memory for MVP.
- Update current_customer_id when user selects or asks about a customer.
- Preserve context for follow-up questions.
- Return context state in /api/chat response.

Do not implement LLM yet.
```

Done khi agent nhớ được khách đang nói tới.

### Phase 4 — Tool registry

Agent dùng: **AI Agent Engineer**

Task:

```
```

```
Implement CRM Tool Registry.

Tools:
- get_customers
- get_customer_by_id
- get_interactions
- get_opportunities
- get_campaigns
- search_product_kb
- search_templates
- write_audit_log

Requirements:
- Each tool returns data plus source IDs.
- Each tool call is logged.
- Tool results must be usable by AgentService.
- Use mock JSON only.

Do not generate email yet.
```

Done khi backend có tool layer rõ ràng, không gọi data lung tung.

### Phase 5 — Rule-based agent MVP

Agent dùng: **AI Agent Engineer**

Task:

```
```

```
Implement AgentService without LLM first.

Supported intents:
1. daily_prioritization
2. customer_summary
3. campaign_match
4. next_best_action

Requirements:
- Detect intent using simple rules/keywords.
- Call relevant tools.
- Return:
  summary
  key_findings
  recommended_action
  sources
  tools_called
  context_state
  requires_approval
  audit_log_id

Use Vietnamese output.
Do not invent data.
```

Done khi câu “Sáng nay tôi nên chăm sóc khách nào trước?” trả được top khách + lý do + source.

Đây là mốc quan trọng nhất. Nếu mốc này chạy, demo đã có nền.

### Phase 6 — Draft email/call script

Agent dùng: **AI Agent Engineer**

Task:

```
```

```
Implement draft generation for email and call script.

Supported intents:
- generate_email
- generate_call_script

Requirements:
- Use customer context.
- Use opportunity data.
- Use campaign/product data.
- Use template data.
- Generate Vietnamese professional banking tone.
- Output draft but do not save automatically.
- Set requires_approval = true.
- Include source IDs and template ID.
- Add risk flags if product eligibility is unclear.
```

Done khi agent tạo được draft có approval.

### Phase 7 — Approval workflow

Agent dùng: **Backend Agent** hoặc **AI Agent Engineer**

Task:

```
```

```
Implement approval workflow.

Endpoints:
- POST /api/draft-email
- POST /api/call-script
- POST /api/approve-action

Requirements:
- Draft can only be saved if approved = true.
- Store approval status in audit log.
- MVP does not send real email.
- Return draft_id or script_id after approval.
- Reject save if approval is missing.
```

Done khi giám khảo thấy agent không tự hành động nguy hiểm.

### Phase 8 — Frontend UI

Agent dùng: **Frontend Agent**

Task:

```
```

```
Build the RMFlow demo UI.

Required sections:
- Chat panel
- Customer context panel
- Tool trace panel
- Source chips
- Prioritized customer list
- Draft preview
- Approve/reject/edit buttons
- Audit log panel
- Demo mode badge
- Metric cards

Connect to FastAPI endpoints.
Do not modify backend.
```

Done khi UI không giống chatbot thường, mà có tool trace/source/audit.

### Phase 9 — Audit log visible

Agent dùng: **Backend Agent + Frontend Agent**

Task Backend:

```
```

```
Ensure every /api/chat request creates an audit log.

Audit fields:
- log_id
- session_id
- user_query
- intent
- tools_called
- source_ids
- model_used
- latency
- output_summary
- approval_status
- created_at
```

Task Frontend:

```
```

```
Show audit logs in a panel.

Requirements:
- Show latest log first.
- Show tools called.
- Show source IDs.
- Show approval status.
- Show latency.
```

Done khi compliance story mạnh.

### Phase 10 — LLM integration, chỉ sau khi rule-based chạy

Agent dùng: **AI Agent Engineer**

Task:

```
```

```
Add LLM generation only for natural language polishing and draft generation.

Rules:
- Agent must retrieve data first.
- LLM must receive only grounded context.
- LLM must not invent missing facts.
- If data is missing, output a warning.
- Keep rule-based scoring separate from LLM wording.
- Log every LLM call.
```

Quan trọng: không dùng LLM để quyết định mọi thứ. Rule-based logic quyết định ưu tiên; LLM chỉ giúp diễn đạt, draft, tóm tắt.

## 7. Build scoring logic đơn giản, đừng ML

Trong brainstorm bạn có scoring: due follow-up + opportunity value + campaign match + sentiment. Hãy code đơn giản:

```
```

```
priority_score =
due_follow_up_score
+ opportunity_value_score
+ probability_score
+ campaign_match_score
+ sentiment_score
```

Ví dụ:

```
```

```
Due today/overdue: +40
Open opportunity high value: +25
Probability > 0.7: +15
Campaign match: +15
Positive sentiment: +5
```

Output phải giải thích được:

```
```

```
KH Nguyễn A được ưu tiên vì:
- Có lịch follow-up đến hạn: I001
- Có opportunity tiết kiệm 12 tháng, expected value 700M: O001
- Phù hợp campaign Summer Savings Boost: CAM001
- Từng quan tâm lãi suất kỳ hạn 12 tháng: I001
```

Đây là điểm ăn tiền vì judge sẽ hỏi: “Tại sao khách này đứng đầu?”

## 8. Bộ câu demo bạn cần chuẩn bị

Bạn chỉ cần 5 câu cho hero demo:

```
```

```
1. Sáng nay tôi nên chăm sóc khách nào trước?

2. Tại sao khách Nguyễn A được ưu tiên?

3. Khách này phù hợp campaign nào và nên đề xuất gì?

4. Tạo cho tôi call script ngắn để gọi khách này.

5. Tôi duyệt script này, lưu vào CRM.
```

Câu số 3 là wow moment vì agent phải nhớ `current_customer_id`, không cần hỏi lại.

## 9. Definition of Done cho MVP

MVP được xem là xong khi chạy được flow này:

```
```

```
RM hỏi tiếng Việt
→ Agent gọi mock CRM tools
→ Agent xếp hạng top khách
→ User chọn 1 khách
→ Agent giữ context khách đó
→ Agent match opportunity + campaign + product KB
→ Agent tạo call script/email
→ UI yêu cầu approval
→ User approve
→ Backend lưu draft mock
→ Audit log hiện tool/source/approval
```

Không có flow này thì chưa xong. Có flow này rồi thì chưa cần branch manager dashboard, real CRM, full MCP, vector DB.

## 10. Cái nên mock và cái không được mock

Nên mock:

```
```

```
Customer data
Interaction history
Opportunity
Campaign
Product KB
Template
POST draft endpoint
Metrics dashboard
Accuracy test cases
```

Không được mock bằng lời nói suông:

```
```

```
Tool trace
Source IDs
Approval status
Audit log
Context state
```

Vì đây là bằng chứng agentic. Judge nhìn thấy là hiểu ngay.

## 11. Cách sử dụng Trae hằng ngày

Mỗi lần làm, bạn theo routine này:

```
```

```
1. Chọn đúng Agent trong Trae
2. Paste task nhỏ
3. Bắt Agent đọc docs liên quan
4. Bắt Agent lập plan trước
5. Cho code
6. Chạy test
7. Nếu lỗi, switch Debugger Agent
8. Chạy được thì commit
9. Switch Reviewer Agent trước khi merge/demo
```

Template task cố định:

```
```

```
## Task
[One task only]

## Context
[Why this task matters]

## Files allowed to modify
[...]

## Do not touch
[...]

## Acceptance Criteria
- ...
- ...
- ...

## Test Checklist
- ...
- ...

## Instruction
Read docs/AI_RULES.md and relevant /agents files first.
Before coding, list affected files and implementation plan.
After coding, explain how to test.
```

Không giao task kiểu:

```
```

```
Build toàn bộ backend cho tôi.
```

Giao kiểu:

```
```

```
Implement only the get_customers and get_interactions mock API endpoints.
```

## 12. Git workflow chống Trae phá code

Trước khi cho Trae sửa lớn:

```
```

```
git status
git add .
git commit -m "checkpoint before agent changes"
```

Sau khi Trae sửa:

```
```

```
git diff
```

Nếu ổn:

```
```

```
git add .
git commit -m "implement mock crm endpoints"
```

Nếu nát:

```
```

```
git restore .
```

Đây là skill bắt buộc. Khi có checkpoint, bạn không còn sợ AI phá project.

## 13. Thứ tự học skill song song

Bạn không chỉ build project, bạn đang build skill. Đi theo thứ tự này:

```
```

```
Skill 1: Đọc và chia spec
Skill 2: Viết task nhỏ
Skill 3: Dùng Trae Rules
Skill 4: Switch custom Agent theo phase
Skill 5: Build mock API
Skill 6: Build tool registry
Skill 7: Build agent context state
Skill 8: Build approval flow
Skill 9: Debug bằng evidence
Skill 10: Review diff và commit
```

Đây là skill thật sự của AI-native developer. Không phải “prompt hay” đơn thuần.

## 14. Lịch build 48 giờ cho team bạn

Nếu team 5 người:

```
```

```
Hour 0–3:
Business chốt product spec + demo script.
IT setup repo + folder + docs.

Hour 3–8:
Business viết 3 hero customers + test cases.
Backend build mock APIs.
Frontend build UI shell.

Hour 8–16:
Backend build tool registry + audit log.
AI engineer build context state + intent detection.
Frontend build chat + customer panel.

Hour 16–24:
Agent flow daily prioritization.
Agent flow customer summary.
Agent flow campaign/product recommendation.

Hour 24–32:
Draft email/call script.
Approval workflow.
Audit panel.

Hour 32–38:
LLM integration if needed.
UI polish.
Fallback mode.

Hour 38–44:
Testing 20 cases.
Bug fixing.
Review.

Hour 44–48:
Demo rehearsal.
Pitch deck.
Backup video.
Final README.
```

Nếu chỉ có bạn tự học một mình, kéo thành 7 ngày:

```
```

```
Day 1: Setup repo + docs + Trae agents
Day 2: Mock data + backend skeleton
Day 3: Tool registry + audit log
Day 4: Agent context + prioritization
Day 5: Draft + approval
Day 6: Frontend UI
Day 7: Debug + pitch/demo
```

## 15. Việc bạn nên làm ngay bây giờ

Làm 5 việc này trước, chưa code logic:

```
```

```
1. Tạo repo rmflow-agent
2. Tạo folder structure như trên
3. Copy brainstorm output vào đúng docs/ và agents/
4. Tạo Project Rule trong Trae
5. Tạo 8 custom Agents trong Trae
```

Sau đó mới bắt đầu task đầu tiên:

```
```

```
Architect Agent → Generate final folder structure and implementation order.
```

Rồi:

```
```

```
Backend Agent → Create mock data.
```

Rồi:

```
```

```
Backend Agent → Create FastAPI skeleton.
```

Đừng nhảy thẳng vào LLM, RAG, MCP hay frontend đẹp. Đầu tiên phải có:

```
```

```
Mock data
→ API
→ Tool registry
→ Context state
→ Rule-based agent
```

